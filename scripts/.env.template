# Copy to scripts/.env (gitignored) and fill in your own key.
# Used by scripts/inspect-gemini.sh as a fallback when GEMINI_API_KEY
# isn't already exported in the shell that launches unspool.

# https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional — defaults to gemini-2.5-flash if unset.
# GEMINI_MODEL=
